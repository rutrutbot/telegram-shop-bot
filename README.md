# Telegram Shop Bot

Бот для продажи оффлайн товаров в разных городах.

## Установка

1. Установите Python 3.9 или выше

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Получите токен бота:
   - Откройте Telegram и найдите @BotFather
   - Отправьте команду `/newbot`
   - Следуйте инструкциям и получите токен

4. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

5. Откройте `.env` и заполните:
```
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_IDS=ваш_telegram_id
```

Чтобы узнать свой Telegram ID:
- Напишите боту @userinfobot
- Он отправит вам ваш ID

## Запуск

```bash
python bot.py
```

## Настройка бота

### 1. Войдите в админ-панель
Отправьте боту команду `/admin`

### 2. Добавьте города
- Нажмите "🏙 Города"
- Используйте команду `/add_city`
- Введите название города и варианты написания

### 3. Добавьте товары
- Нажмите "📦 Товары"
- Используйте команду `/add_product`
- Выберите город, введите название, иконку и цену

### 4. Добавьте районы
- Нажмите "📍 Районы"
- Используйте команду `/add_district`
- Выберите город и введите название района

### 5. Настройте платежи
- Нажмите "⚙️ Настройки"
- `/set_card` - номер карты для оплаты
- `/set_payment_instruction` - инструкция по оплате
- `/set_operator` - ссылка на оператора (например: https://t.me/username)
- `/set_success_message` - сообщение после оплаты
- `/set_timeout_message` - сообщение при истечении времени

## Функционал

### Для клиентов:
1. Выбор города
2. Выбор товара
3. Выбор района
4. Выбор способа оплаты (карта/Bitcoin)
5. Оплата в течение 30 минут
6. Связь с оператором

### Для администраторов:
- Управление городами
- Управление товарами
- Управление районами
- Настройка платежей
- Настройка сообщений

## Структура проекта

```
telegram-shop-bot/
├── bot.py              # Основной файл
├── config.py           # Конфигурация
├── database.py         # База данных
├── keyboards.py        # Клавиатуры
├── handlers/
│   ├── client.py       # Обработчики клиентов
│   └── admin.py        # Обработчики админа
├── requirements.txt    # Зависимости
└── .env               # Настройки (создать вручную)
```

## Поддержка

При возникновении проблем проверьте:
1. Правильность токена в `.env`
2. Правильность ID администратора
3. Наличие всех зависимостей
4. Логи в консоли
